# Homepage Configuration Tool Environment Variables

# Authentication Settings (Required - Must be set!)
AUTH_USERNAME=admin
AUTH_PASSWORD=changeme

# Session Settings
SESSION_SECRET=change-this-to-a-random-secret-key-for-production
SESSION_EXPIRE_MINUTES=1440

# Application settings
APP_NAME="Homepage Configuration Tool"
APP_VERSION="1.0.0"
DEBUG=true

# File paths
CONFIG_PATH="config/services.yaml"
EXAMPLE_CONFIG_PATH="config/example.yaml"
UPLOAD_DIR="uploads"

# API settings
API_PREFIX="/api"

# Frontend settings
FRONTEND_PATH="frontend"

# CORS settings (comma-separated origins)
CORS_ORIGINS="*"